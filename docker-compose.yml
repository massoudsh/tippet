services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: tipet
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d tipet"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
