# AGENTS.md — تیپت

راهنمای کار روی این مخزن برای هر ایجنت/توسعه‌دهنده‌ی بعدی.

## ویکی دانش پروژه (`docs/wiki/`)

این پروژه یک «ویکی دانش» زنده و append-friendly کنار کد دارد که به‌جای re-scan کردن کل کدبیس در هر
مکالمه، خلاصه‌ی entity ها (مدل‌ها، سرویس‌ها) و concept ها (flowها، تصمیم‌های معماری) را نگه می‌دارد.
مسیر: `docs/wiki/`. همان محتوا روی GitHub Wiki (`https://github.com/massoudsh/tippet/wiki`) هم منتشر
می‌شود — هر دو باید هم‌زمان به‌روز بمانند.

### شروع هر مکالمه/سشن
1. اول `docs/wiki/overview.md` و `docs/wiki/index.md` را بخوان (هزینه کم). هرگز کل کدبیس را re-scan نکن.
2. فقط صفحات مرتبط با تسک فعلی را drill-down کن (`docs/wiki/entities/*.md`, `docs/wiki/concepts/*.md`).
3. `docs/PRD.md` و `docs/DATA_MODEL.md` اسناد مرجع اصلی‌اند؛ فقط وقتی ویکی برای تسک کافی نبود سراغشان برو.

### وقتی کد را تغییر دادی — قانون اکید
هر بار که یک فایل پروژه را Write/Edit می‌کنی (خارج از `docs/wiki/` خودش):
1. تشخیص بده این تغییر روی کدام entity/concept اثر می‌گذارد.
2. صفحه‌ی مربوطه در `docs/wiki/` را به‌روز کن (فیلد جدید/حذف‌شده، امضای تابع، route جدید، edge case).
3. اگر entity/concept کاملاً جدید و به‌اندازه کافی مهم است (≥۳ ارجاع یا حجم مستقل)، صفحه جدید بساز و در
   `docs/wiki/index.md` لینک بده. در ۹۰٪ موارد به‌جای فایل جدید، صفحه‌ی موجود را آپدیت کن.
4. یک خط در `docs/wiki/log.md` اضافه کن: `## [YYYY-MM-DD] update | <خلاصه تغییر>`.
5. همان تغییر فایل را روی GitHub Wiki هم اعمال کن (push به `tippet.wiki` — همان مسیر پوشه‌ها).
6. اگر تغییر یک فیچر/فاز جدید معرفی کرد، Issue/Milestone متناظر در گیت‌هاب هم بساز یا آپدیت کن و در
   `docs/wiki/concepts/roadmap-phases.md` لینک بده.

تغییرات trivial (typo، فرمت، rename بدون تغییر امضا) نیاز به آپدیت ویکی ندارند. اگر چند فایل در یک
مکالمه تغییر کردند، آپدیت‌های ویکی را یک‌بار در پایان مکالمه انجام بده، نه بعد از هر فایل.

### قواعد لینک (Obsidian-style)
- `[[entities/item]]` بدون پسوند `.md`.
- نام فایل: lowercase + hyphen.

### Lint دوره‌ای
وقتی کاربر گفت «ویکی رو بازبینی کن»: orphan pages، broken link، تناقض بین صفحات، صفحات کهنه را پیدا کن،
اصلاح کن (بدون پرسیدن، بهترین حدس را انتخاب کن) و در `docs/wiki/log.md` ثبت کن.

قوانین کامل و دقیق‌تر در [`docs/wiki/schema.md`](docs/wiki/schema.md).

## بیلد و اجرا
نصب پکیج و build سنگین (`npm install`, `npm run build`) داخل کانتینر/محیط ساخت انجام نشود — روی سرور
SSH طبق `web/README.md` انجام شود.
